# =============================================================================
# WeJob 环境变量示例
# =============================================================================
# 复制此文件为 `.env.local`（本地开发）填入真实值。
# 部署到 Vercel 时，请在 Vercel 控制台 → Project → Settings → Environment Variables
# 配置；不要把真实值提交到 git。
# =============================================================================

# ⚠️ 安全约定（必读）
# - 这些环境变量**只在 Vercel 服务端 / Next.js Server Runtime** 使用。
# - 切勿在变量名加 `NEXT_PUBLIC_` 前缀 —— 那样会内联到浏览器 bundle，泄露 key。
# - 切勿 commit `.env.local` 到 git（.gitignore 已忽略 .env / .env.local / .env*.local）。
# - 本地开发：在仓库根目录新建 `.env.local`，填入真实值，文件不会被 git 追踪。
# - 生产/Preview：在 Vercel Dashboard 配；CLI 用 `vercel env add <NAME> production`。
# - Key 泄露后立即在控制台撤销 + 重新生成 —— 详见 ./SECURITY.md。

# ---- AI 服务（LongCat，兼容 OpenAI 协议）----
# 用于简历分析、简历优化、模拟面试题生成、面试回答评估等 AI 功能。
# 服务端 src/app/api/ai/chat/route.ts 通过 @ai-sdk/openai 的 createOpenAI
# 调 https://api.longcat.chat/openai/v1，baseURL/model 走默认。
LONGCAT_API_KEY=ak_your_longcat_key_here

# 可选：自定义 baseURL（一般用默认）
# LONGCAT_API_BASE=https://api.longcat.chat/openai/v1

# 可选：自定义模型名（默认 LongCat-2.0-Preview）
# LONGCAT_MODEL=LongCat-2.0-Preview

# ---- 数据源 ----
# 薪资行情数据目前硬编码在 src/lib/data.ts，无外部依赖。
# 如未来接入第三方薪资 API，在此新增。

# ---- 站点配置 ----
# 公开站点 URL，用于 SEO、canonical、OG 标签等。
# 生产环境为 https://wejob-gold.vercel.app
NEXT_PUBLIC_SITE_URL=https://wejob-gold.vercel.app

# ---- 分析与监控（可选）----
# Google Analytics 4 测量 ID（如使用 GA4）
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Vercel Analytics 自动启用，无需手动配置；如要关闭请在 next.config.js 移除相关组件。

# ---- 备注 ----
# 1. 以 NEXT_PUBLIC_ 开头的变量会暴露到浏览器端；非该前缀的变量仅服务端可用。
# 2. Next.js 14 不会自动注入未声明的 env；新建变量后需重启 `next dev` 或重新部署。
# 3. 生产环境变量优先级：Vercel Dashboard > .env.production > .env.local > 默认值。
# 4. LONGCAT_API_KEY 永远不要出现在客户端代码、commit history、Vercel 公开日志里。
